# Dependencies
node_modules/
**/node_modules

# Build output
dist/
**/dist
*.tsbuildinfo

# Test / coverage / e2e
coverage/
playwright/
.playwright/
playwright-report/
test-results/

# Env & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS cruft
.DS_Store
Thumbs.db
.idea/
*.swp

# Local Claude Code config & transient agent worktrees (not for the shared repo)
.claude/

# Internal planning & AI-session instructions — kept local, not published
docs/
CLAUDE.md
AGENTS.md

# T-1505: print-resolution raw source STL models (~35 MB) — never shipped. The optimized/decimated
# copies actually loaded by the client (`apps/client/scripts/optimize-models.mjs`'s output) live in
# apps/client/src/board3d/models/opt/ and ARE committed.
apps/client/src/board3d/models/raw/
