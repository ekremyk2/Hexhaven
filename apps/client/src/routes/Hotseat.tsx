// `/hotseat` route entry (T-305). The actual harness lives under `src/hotseat/**` (the task's
// deliverables) so it stays a self-contained module — this file only wires it to the router.
export { default } from '../hotseat/HotseatPage';
